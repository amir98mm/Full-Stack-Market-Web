export class User{
    constructor(public username?:string, public password?:string, public email?:string, public first?:string, public last?:string, public phoneNumber?:string, public gender?:string, public birthday?:string)
    {
  
    }
  }